[build]
builder = "DOCKERFILE"
dockerfilePath = "dashbord_snkrdunk/Dockerfile"
buildContext = "dashbord_snkrdunk"

[deploy]
startCommand = "streamlit run dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
